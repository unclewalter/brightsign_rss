version: '3.8'
services:
  appcontainer: 
    build: 
      context: .
      dockerfile: Dockerfile
    volumes: 
      - ./media:/usr/share/media
    ports: 
      - 8080:8080
    environment:
    - HOSTNAME=10.224.10.53
    - PORT=8080
    